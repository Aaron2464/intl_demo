name: demo
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.16.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.2


  intl: ^0.17.0
  intl_utils: ^2.6.1
  stream_chat_flutter: ^3.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  build_runner: ^2.1.5

flutter:
  uses-material-design: true

flutter_intl:
  enabled: true
  arb_dir: lib/core/i18n/arb
  output_dir: lib/core/i18n/gen